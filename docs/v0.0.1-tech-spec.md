# OpenTodo v0.0.1

The desktop-first implementation plan is maintained in the [GitHub milestone description](https://github.com/raeperd/opentodo/milestone/1). Update that description with `gh` CLI; this file preserves existing links and is not a separate spec.
