interface:
  display_name: "OpenTodo Reference Research"
  short_description: "Research reference apps and dependency examples"
  default_prompt: "Use $opentodo-reference-research to answer the implementation question from relevant reference repositories or version-matched dependency examples using fresh temporary shallow clones, fetching history only as needed, then return commit-pinned evidence and a concise recommendation for OpenTodo."
